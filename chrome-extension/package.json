{
  "name": "tailorcv-extension",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/styles.js --bundle --format=iife --loader:.css=text --outfile=styles.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
