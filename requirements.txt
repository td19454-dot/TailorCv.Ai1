# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
jinja2>=3.1.2
aiofiles>=23.2.1
Pillow

email-validator>=2.0.0
psycopg2-binary>=2.9.9
itsdangerous>=2.0.0
sqlalchemy
pgvector>=0.5.0
bs4
# Google Sign-In
google-auth>=2.29.0

# AI/ML dependencies
openai>=1.3.0
anthropic>=0.34.0
tenacity>=8.5.0

# PDF processing
pdfplumber>=0.10.0
weasyprint>=60.0
pymupdf
spacy
pypdf>=4.0.0
passlib
# Markdown processing
markdown>=3.5.0

# Production dependencies 
# jupyterlab
# ipykernel
# ipywidgets
# gradio
httpx>=0.27.0
resend
#Analytics
posthog

# Error tracking
sentry-sdk[fastapi]>=2.0.0

# Payments
razorpay>=1.4.0
polar-sdk>=0.1.0

# Auto-apply (local browser automation via Stagehand + Playwright — no
# Browserbase account/session needed). >=4.0.0 is required: that's the first
# version exposing stagehand.local_browser, which auto_apply/browser.py uses
# to launch its own Chromium instead of connecting to a Browserbase session.
# The build MUST also run `playwright install chromium --with-deps` — unlike
# the old Browserbase-backed setup, this now launches a real local browser.
stagehand>=4.0.0
playwright>=1.47.0
